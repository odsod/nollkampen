li: a(href='/sections/' + section.id)
  h3(style='background: ' + section.color + '; color: ' + section.textColor)= section.initials
  p.section-initials= section.name
  a(href='#delete' + section.id, data-rel='popup',
    data-position-to='window') Ta bort
