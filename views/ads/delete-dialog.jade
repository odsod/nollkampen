.ui-corner-all(data-role='popup', id='delete' + ad.id)
  .ui-corner-top(data-role='header'): h1 Ta bort annons
  .ui-corner-bottom(data-role='content')
    h4 Vill du verkligen ta bort #{ad.name}?
    p Du kan inte ångra dig i efterhand.
    form(method='POST', action='/ads/' + ad.id + '/delete')
      include ../includes/form-controls-delete
